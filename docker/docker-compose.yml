---
services:
  bullseye:
    build:
      context: ..
      dockerfile: docker/debian/Dockerfile
      args:
        OS: debian:bullseye
  bookworm:
    build:
      context: ..
      dockerfile: docker/debian/Dockerfile
      args:
        OS: debian:bookworm
  trixie:
    build:
      context: ..
      dockerfile: docker/debian/Dockerfile
      args:
        OS: debian:trixie
  rockylinux10:
    build:
      context: ..
      dockerfile: docker/rpm/Dockerfile
      args:
        OS: rockylinux:10
